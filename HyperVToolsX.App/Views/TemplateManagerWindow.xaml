<Window x:Class="HyperVToolsX.App.Views.TemplateManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Custom Tabs"
        Icon="/Resources/logo.png"
        Width="620"
        Height="420"
        MinWidth="520"
        MinHeight="320"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="#F2F2F2">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="FolderText" Grid.Row="0" Foreground="#666666" TextWrapping="Wrap" Margin="0,0,0,8"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ListView x:Name="TemplateListView"
                      SelectionMode="Single"
                      MouseDoubleClick="TemplateListView_MouseDoubleClick">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" Width="230" DisplayMemberBinding="{Binding Name}"/>
                        <GridViewColumn Header="Rows" Width="150" DisplayMemberBinding="{Binding Rows}"/>
                        <GridViewColumn Header="Columns" Width="80" DisplayMemberBinding="{Binding ColumnCount}"/>
                    </GridView>
                </ListView.View>
            </ListView>

            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <Button Content="New..." Width="90" Padding="6,4" Margin="0,0,0,6" Click="NewButton_Click"/>
                <Button Content="Edit..." Width="90" Padding="6,4" Margin="0,0,0,6" Click="EditButton_Click"/>
                <Button Content="Delete" Width="90" Padding="6,4" Click="DeleteButton_Click"/>
            </StackPanel>
        </Grid>

        <Button Grid.Row="2" Content="Close" Width="90" Padding="6,4" IsCancel="True"
                HorizontalAlignment="Right" Margin="0,12,0,0"/>
    </Grid>
</Window>
